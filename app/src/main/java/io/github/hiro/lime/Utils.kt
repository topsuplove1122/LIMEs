package io.github.hiro.lime

import android.content.Context

object Utils {
    /**
     * 暫時留空，避免其他檔案引用出錯。
     * 在 Java 中調用方式為：Utils.addAssetPath(context);
     * 在 Kotlin 中調用方式為：Utils.addAssetPath(context)
     */
    @JvmStatic
    fun addAssetPath(context: Context) {
        // 待實作
    }
}
